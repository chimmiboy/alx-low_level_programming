#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * Main - Entry point
 *
 * Return; This returns 0
 */

int main(void)
{
	int n;
	int lastN;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	l = n % 10;

	if(lastN > 5)
	{
		printf("Last digit of %d is %d and is greater than 5\n", n, last
		       N);
	}
	else if
	{}