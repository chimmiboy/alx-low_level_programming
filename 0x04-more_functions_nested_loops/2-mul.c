#include "main.h"

/**
 * mul -  a function that multiplies two integers a and b
 * @a: first input
 * @b: second input
 * Return: 1 if c is a digit, 0 otherwise
 */

int mul(int a, int b)
{
	return (a * b);
}
